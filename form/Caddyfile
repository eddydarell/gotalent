# Caddyfile for reverse proxy
{
    auto_https off
}

:80 {
    # Frontend
    handle / {
        reverse_proxy frontend:80
    }

    # API routes
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Health check
    handle /health {
        respond "OK" 200
    }
}
